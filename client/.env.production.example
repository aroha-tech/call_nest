# Copy to .env.production and set before running: npm run build
# Used when building for production (vite build loads .env.production)

# API base URL: your production API (tenant subdomain), e.g. https://acme.arohva.com
VITE_API_BASE_URL=https://acme.arohva.com
