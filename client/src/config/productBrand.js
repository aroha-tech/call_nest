/** User-visible product name (browser title, billing, invoices, platform shell). Server mirror: `server/src/config/productBrand.js`. */
export const PRODUCT_DISPLAY_NAME = 'CallXTime';

/** In-app AI name (lead routing, coaching copy). Short “X” ties to CallXTime. */
export const AI_ASSISTANT_DISPLAY_NAME = 'nest AI';

/** Advanced reports: on-server guidance panel title. */
export const AI_INSIGHTS_DISPLAY_NAME = 'nest Insights';
